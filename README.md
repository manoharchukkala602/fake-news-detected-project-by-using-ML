## Funtion :
its can detected if news is fake or true
its have full securable pattern
good quality worth
## Features :
its have input direct to user 
detectable fastly 
its provide accuracy_score
## using modules:
## LogisticRegression()
it have LogisticRegression() module which import from sklearn.linear_module,
its main fucntion predict the fake news in this project.
 ## tfidCountervectorize()
it have tfidCountervectorize() module which is import from sklearn.feature.extraction.text,
its main fuction convert text into numeric values.
## accuracy_score()
it have accuracy_score module which import from sklearn.metrece,
its main fucntion calculationa and classification .
## pandas()
it have pandas library which makes the data in order as frame .
## train_test_split()
it have the train_test_split() module which import sklearn.model_selection,
it main fucntion which is train and test the data .
## csv:
its have fake csv and true csv files.
## auther :
manohar chukkala



